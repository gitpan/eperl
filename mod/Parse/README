
  This is the Perl 5 glue code which includes the
  ePerl parser into a Perl module Parse::ePerl
  which can be used to transform a bristled source
  code buffer into an expanded source code buffer.

